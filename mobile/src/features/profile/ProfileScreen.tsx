import { FeaturePlaceholderScreen } from '@/shared/ui/components/FeaturePlaceholderScreen';

export function ProfileScreen() {
  return (
    <FeaturePlaceholderScreen
      body="User preferences, session controls, and role-aware settings can expand here while the mobile shell continues to enforce protected navigation."
      eyebrow="Profile"
      title="Operator profile surface"
    />
  );
}
